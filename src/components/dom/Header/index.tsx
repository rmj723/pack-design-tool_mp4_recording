import s from './Header.module.scss'
import PuckToggle from '@/components/dom/PuckToggle'

const Header = () => {
  return <div className={`${s.container}`}>{/* <PuckToggle /> */}</div>
}

export default Header
